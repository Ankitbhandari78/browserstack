# Check the status of your files to see untracked or modified changes
git status

# Stage all new, modified, or deleted files for the next commit
git add .

# Save a snapshot of your staged changes with a descriptive message
git commit -m "Your commit message"

# Upload your local commits to the remote branch (e.g., main)
git push origin main

>>>>>>>>>>>>>>
file name particular

# Check the status of your files to see untracked or modified changes
git status

# Stage all new, modified, or deleted files for the next commit
git add .

# Save a snapshot of your staged changes with a descriptive message
git commit -m "Your commit message"

# Upload your local commits to the remote branch (e.g., main)
git push origin main

>>>>>>>>>
to execute all test write (npx playwright e2e)
# 1. Run in Headed mode (opens the visible browser window so you can wsatch execution)
npx playwright test --headed

# 2. Run on a specific browser (Chromium, Firefox, or WebKit)
npx playwright test --project=chromium

# 3. Debug mode (opens Playwright Inspector to step through line-by-line)
npx playwright test --debug

# 4. View HTML Test Report after execution
npx playwright show-report

>>>>>>>

npx playwright codegen --channel=chrome https://www.google.com
